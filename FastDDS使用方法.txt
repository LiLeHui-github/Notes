1. 使用CMake配置FastDDS库依赖
2. 创建消息数据结构HelloMsg.idl
    struct HelloMsg
    {
        unsigned long index;
        string message;
    };
3. 生成代码
   fastddsgen.bat -language c++ -ppDisable HelloMsg.idl

使用动态库
    set(fastdds_SHARED_LIBS ON)
    set(fastcdr_SHARED_LIBS ON)

    配置宏
    EPROSIMA_ALL_DYN_LINK