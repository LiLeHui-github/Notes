sudo apt-get update

下载后缓存路径
/var/cache/apt/archives

下载包
sudo apt-get install --download-only --reinstall <pakcage_name>

清理缓存
sudo apt cleanG

安装包
sudo dpkg -i *.deb
sudo apt install ./<package_name>

或者在干净系统调用安装然后复制缓存
    sudo apt clean
    然后去  /var/cache/apt/archives 复制所有包